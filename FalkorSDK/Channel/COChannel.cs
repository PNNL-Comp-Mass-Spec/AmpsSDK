namespace FalkorSDK.Channel
{
    public class COChannel : Channel
    {
	    public COChannel(int address, string name, string description)
		    : base(address, name, description)
	    {
	    }
    }
}