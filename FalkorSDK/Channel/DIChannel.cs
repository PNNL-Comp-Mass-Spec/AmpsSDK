namespace FalkorSDK.Channel
{
    public class DIChannel : Channel
    {
	    public DIChannel(int address, string name, string description)
		    : base(address, name, description)
	    {
	    }
    }
}